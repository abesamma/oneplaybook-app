import React from 'react';
import LightBrowserFrame from '../assets/browser_frame_light.png';

const HeroImage = () => (
  <div>
    <img className="device-content" alt="screenshot of app" src={LightBrowserFrame} />
  </div>
);

export default HeroImage;
