import React from 'react';
import svg from '../../assets/dropbox.svg';

const Dropbox = ({ className }) => (
  <>
    <img className={className} src={svg} alt="Dropbox logo" />
  </>
);

export default Dropbox;
